<form method="post" action="{{ path('roles_delete', {'id': role.id}) }}"
      onsubmit="return confirm('Voulez-vous supprimer vraiment ce rôle?\nVous risquez de perdre toutes les informations sur ce dernier');"
      style="display: inline-block">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role.id) }}">
    <button class="btn btn-xs btn-danger"><i class="fa fa-trash"></i> Supprimer </button>
</form>
