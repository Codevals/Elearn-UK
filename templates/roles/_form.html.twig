{{ form_start(form) }}
<div class="row">
    <div class="col-md-5">{{ form_row(form.name) }}</div>
</div>
<div class="row">
    <div class="col-md-5">{{ form_row(form.slug) }}</div>
</div>
<div class="row">
    <div class="col-md-5">{{ form_row(form.level) }}</div>
</div>
<button class="btn">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
