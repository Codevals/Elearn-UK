{% extends 'adminTemplate/baseAdmin.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1>Admin</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ admin.id }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ admin.nom }}</td>
        </tr>
        <tr>
            <th>Prenom</th>
            <td>{{ admin.prenom }}</td>
        </tr>
        <tr>
            <th>ContactCel</th>
            <td>{{ admin.contactCel }}</td>
        </tr>
        <tr>
            <th>ContactMail</th>
            <td>{{ admin.contactMail }}</td>
        </tr>
        <tr>
            <th>Username</th>
            <td>{{ admin.username }}</td>
        </tr>
        <tr>
            <th>Password</th>
            <td>{{ admin.password }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ admin.type }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_list') }}" class="btn btn-default btn-xs">
        <i class="fa fa-list-alt"></i>
        Retour à la liste des admins
    </a>

    <a href="{{ path('admin_edit', {'id': admin.id}) }}" class="btn btn-warning btn-xs">
        <i class="fa fa-edit"></i>
        Modifier
    </a>

    {{ include('admin/_delete_form.html.twig') }}
{% endblock %}
