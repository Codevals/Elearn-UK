{% extends 'adminTemplate/baseAdmin.html.twig' %}

{% block title %}Enseignant{% endblock %}

{% block body %}
    <h1>Enseignant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseignant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ enseignant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ enseignant.prenom }}</td>
            </tr>
            <tr>
                <th>ContactCel</th>
                <td>{{ enseignant.contactCel }}</td>
            </tr>
            <tr>
                <th>ContactMail</th>
                <td>{{ enseignant.contactMail }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ enseignant.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ enseignant.password }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ enseignant.titre }}</td>
            </tr>
            <tr>
                <th>TitreAbrege</th>
                <td>{{ enseignant.titreAbrege }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('enseignant_index') }}" class="btn btn-default btn-xs">
        <i class="fa fa-list-alt"></i>
        Retour à la liste des enseignants
    </a>

    <a href="{{ path('enseignant_edit', {'id': enseignant.id}) }}" class="btn btn-warning btn-xs">
        <i class="fa fa-edit"></i>
        Modifier
    </a>

    {{ include('enseignant/_delete_form.html.twig') }}
{% endblock %}
